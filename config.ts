  const baseUrl = process.env.MODMED_BASE_URL!;
  const firmUrlPrefix = process.env.FIRM_URL_PREFIX!;
  const accessToken = process.env.MODMED_ACCESS_TOKEN!;
  const api_key = process.env.MODMED_API_KEY!;

export {
    baseUrl,
    firmUrlPrefix,
    accessToken,
    api_key
  }
